# Orphan Document

This document is present on disk but is not listed in the navigation contract
defined in `zenzic.toml`. Zenzic emits Z406 NAV_CONTRACT for this file.

The navigation contract violation is intentional — this file demonstrates
how Z406 detects documents that exist in `docs/` but are unreachable
from the declared site structure.
