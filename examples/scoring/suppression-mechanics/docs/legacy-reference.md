# Legacy Reference

This page's Z601 findings are suppressed via `governance.per_file_ignores`
in `zenzic.toml` (not via inline comments).

The AncientBrand system was decommissioned in 2024. All references here are
intentional historical context. No inline suppress comments are needed because
the per-file rule applies to this entire file.

## Architecture Overview

The AncientBrand platform was built on a monolithic architecture.
The current system replaces it with a modular approach.
