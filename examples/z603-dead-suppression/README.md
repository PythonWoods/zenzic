# Z603 Dead Suppression

This example demonstrates a Z603 DEAD_SUPPRESSION violation.
An inline `zenzic:ignore` directive is present for a finding that is already suppressed by a global policy in `.zenzic.toml`, rendering the inline directive redundant.
