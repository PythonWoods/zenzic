# Setup

Install dependencies and run checks:

```bash
uv add --dev zenzic
zenzic check all
```

This page exists to validate nested path resolution and anchor-safe link checks.
It also demonstrates that code snippets are parsed in pure Python mode and can
be linted without executing commands, preserving deterministic and secure
analysis for contributors and CI systems.
