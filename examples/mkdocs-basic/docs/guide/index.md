# Guide

The guide section is defined as a nested nav block.

This page validates that section containers and child pages are interpreted
consistently with MkDocs 1.x rules, including stable URL mapping and previous
next navigation derivation from declared nav order rather than filesystem order.

Continue with [Setup](setup.md) or return [Home](../index.md).
